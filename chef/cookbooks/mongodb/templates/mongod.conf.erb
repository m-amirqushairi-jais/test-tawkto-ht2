storage:
  dbPath: <%= node['mongodb']['data_dir'] %>

net:
  port: <%= node['mongodb']['port'] %>
  bindIp: <%= node['mongodb']['bind_ip'] %> 

security:
  authorization: enabled

replication:
  replSetName: rs0
